//#define _CRT_SECURE_NO_WARNINGS 1
//#include <stdio.h>
//
//int Max(int x, int y)  // Change return type to int
//{
//    return x > y ? x : y;
//}
//
//int main()
//{
//    int arr[] = { 12, 35, 67, 23, 11, 10 };
//    int len = sizeof(arr) / sizeof(arr[0]);
//    printf("Array length: %d\n", len);
//
//    int max_val = arr[0];  // Initialize max_val with the first element of the array
//
//    for (int i = 1; i < len; i++)  // Start from index 1
//    {
//        max_val = Max(max_val, arr[i]);  // Update max_val with the maximum of current and arr[i]
//    }
//
//    printf("Max value: %d\n", max_val);
//
//    return 0;
//}
